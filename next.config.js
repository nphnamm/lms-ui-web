/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["res.cloudinary.com", "lh3.googleusercontent.com", "randomuser.me","https://lms-ui-web.vercel.app"],
    },
};

module.exports = nextConfig;
